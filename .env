SECRET_KEY="django-insecure-(blamidhaljbvx##xfp5g=epc#m4_5p(!*yw*cw53=z^-exwe#"
DEBUG=1

# Hosts allowed (no scheme). Use * only for local dev
ALLOWED_HOSTS="*"

# CORS settings for local dev + prod frontend
CORS_ALLOW_ALL_ORIGINS=1
CORS_ALLOWED_ORIGINS="http://localhost:3000,https://www.chicago3dprintworks.com"
CSRF_TRUSTED_ORIGINS="http://localhost:3000,https://www.chicago3dprintworks.com"
CORS_ALLOW_CREDENTIALS=1

# Large file upload cap (MB)
DATA_UPLOAD_MAX_MB=200

# Pricing knobs
BASE_FEE=5.0
POSTPROCESS_FEE=0.0
HOURLY_RATE_FALLBACK=8.0
DEFAULT_MACHINE_KEY="BLENDED"

# (Batch vars not used in current endpoint, safe to keep for future)
BATCH_TIERS="1,10,25,50,100"
DISCOUNTS="0,0.05,0.08,0.12,0.15"
